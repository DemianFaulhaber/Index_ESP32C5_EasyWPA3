name=IndexSecureConnection
version=1.0.0
author=DemianF
maintainer=DemianF
sentence=Arduino-friendly wrapper for secure Wi-Fi enterprise (WPA3) and HTTPS on ESP32.
paragraph=Simple API: begin(ssid, identity, password), get(url), post(url, body). Requires arduino-esp32 board package (https://github.com/espressif/arduino-esp32) which bundles the ESP-IDF framework used internally.
category=Communication
url=https://example.com
architectures=esp32
includes=IndexSecureConnection.h
depends=Arduino (>=1.0.0)
